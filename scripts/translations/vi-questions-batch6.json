{
  "translations": [
    {
      "questionId": "a8d3e1f7-0912-4b5c-9c6d-2e8f4a3b1c5d",
      "locale": "vi",
      "title": "useRef vs useState: khi nào dùng refs để avoid re-renders",
      "content": "So sánh useRef và useState. Khi nào nên dùng refs thay vì state?",
      "answer": "**useState** — Triggers re-render:\n```tsx\nconst [count, setCount] = useState(0);\nsetCount(1); // Component re-renders\n```\n\n**useRef** — No re-render:\n```tsx\nconst countRef = useRef(0);\ncountRef.current = 1; // No re-render\n```\n\n**Khi dùng useRef**:\n\n1. **DOM references**:\n```tsx\nconst inputRef = useRef<HTMLInputElement>(null);\n<input ref={inputRef} />\ninputRef.current?.focus();\n```\n\n2. **Mutable values không cần re-render**:\n```tsx\nconst intervalRef = useRef<number>();\n\nuseEffect(() => {\n  intervalRef.current = setInterval(() => {\n    // Do something\n  }, 1000);\n  return () => clearInterval(intervalRef.current);\n}, []);\n```\n\n3. **Previous value tracking**:\n```tsx\nfunction usePrevious<T>(value: T): T | undefined {\n  const ref = useRef<T>();\n  useEffect(() => {\n    ref.current = value;\n  });\n  return ref.current;\n}\n```\n\n4. **Avoiding stale closures**:\n```tsx\nconst callbackRef = useRef(callback);\nuseEffect(() => {\n  callbackRef.current = callback;\n});\n\nuseEffect(() => {\n  const id = setInterval(() => {\n    callbackRef.current(); // Always latest callback\n  }, 1000);\n  return () => clearInterval(id);\n}, []); // No dependency needed\n```\n\n**So sánh**:\n| Feature | useState | useRef |\n|---------|----------|--------|\n| Re-render | Yes | No |\n| Persists | Yes | Yes |\n| Sync | Async (batched) | Sync |\n| Use case | UI state | Mutable values, DOM |\n\n**Rule of thumb**: Nếu value không cần hiển thị trong UI, dùng useRef."
    },
    {
      "questionId": "b7c2e8f4-1234-5678-90ab-cdef12345678",
      "locale": "vi",
      "title": "useMemo và useCallback: khi nào thực sự cần optimize và khi nào là premature",
      "content": "Khi nào nên dùng useMemo và useCallback? Khi nào là premature optimization?",
      "answer": "**useCallback** — Memoize function:\n```tsx\nconst handleClick = useCallback(() => {\n  doSomething(id);\n}, [id]);\n```\n\n**useMemo** — Memoize value:\n```tsx\nconst sortedItems = useMemo(() => {\n  return items.sort((a, b) => a.name.localeCompare(b.name));\n}, [items]);\n```\n\n**Khi THỰC SỰ cần**:\n\n1. **Pass callback to memoized child**:\n```tsx\nconst handleDelete = useCallback((id) => {\n  deleteItem(id);\n}, [deleteItem]);\n\n<MemoizedList onDelete={handleDelete} />\n```\n\n2. **Expensive computations**:\n```tsx\nconst filtered = useMemo(() => {\n  return items.filter(complexFilter).map(complexTransform);\n}, [items]);\n```\n\n3. **Dependency of useEffect**:\n```tsx\nconst options = useMemo(() => ({ page, limit }), [page, limit]);\n\nuseEffect(() => {\n  fetchData(options);\n}, [options]);\n```\n\n**Khi KHÔNG cần** (premature optimization):\n\n```tsx\n// Bad - simple calculation\nconst doubled = useMemo(() => count * 2, [count]);\n\n// Bad - function not passed to memoized child\nconst handleClick = useCallback(() => {\n  setCount(c => c + 1);\n}, []);\n<button onClick={handleClick}>Click</button>  // button not memoized\n\n// Bad - reference equality không matter\nconst style = useMemo(() => ({ color: 'red' }), []);\n<div style={style}>  // inline object cũng OK\n```\n\n**Rule of Thumb**:\n- Measure first với React DevTools Profiler\n- useMemo cho expensive calculations\n- useCallback khi passing to React.memo children\n- Don't memoize everything by default"
    },
    {
      "questionId": "c9d8e7f6-5432-1098-76ba-fedcba098765",
      "locale": "vi",
      "title": "Giải thích PostgreSQL JSONB: indexing, operators, và khi nào dùng thay vì normalized tables",
      "content": "PostgreSQL JSONB hoạt động như thế nào? Khi nào nên dùng JSONB thay vì normalized tables?",
      "answer": "**JSONB Basics**:\n```sql\nCREATE TABLE products (\n  id SERIAL PRIMARY KEY,\n  name VARCHAR(255),\n  attributes JSONB\n);\n\nINSERT INTO products (name, attributes) VALUES (\n  'Laptop',\n  '{\"brand\": \"Dell\", \"specs\": {\"ram\": 16, \"storage\": 512}}'\n);\n```\n\n**Operators**:\n```sql\n-- Access\nSELECT attributes->>'brand' FROM products;  -- text\nSELECT attributes->'specs'->'ram' FROM products;  -- jsonb\n\n-- Contains\nSELECT * FROM products WHERE attributes @> '{\"brand\": \"Dell\"}';\n\n-- Key exists\nSELECT * FROM products WHERE attributes ? 'brand';\nSELECT * FROM products WHERE attributes ?& array['brand', 'specs'];\n\n-- Path exists\nSELECT * FROM products WHERE attributes #> '{specs,ram}' IS NOT NULL;\n```\n\n**Indexing**:\n```sql\n-- GIN index for containment\nCREATE INDEX idx_attributes ON products USING GIN (attributes);\n\n-- GIN with jsonb_path_ops (smaller, faster for @>)\nCREATE INDEX idx_attributes ON products USING GIN (attributes jsonb_path_ops);\n\n-- Expression index for specific key\nCREATE INDEX idx_brand ON products ((attributes->>'brand'));\n```\n\n**Khi dùng JSONB**:\n- Schema-less / flexible data\n- Rarely queried nested fields\n- External API responses\n- User preferences, settings\n- Audit logs, metadata\n\n**Khi dùng Normalized Tables**:\n- Frequently queried fields\n- Need referential integrity\n- Aggregations trên specific fields\n- Data relationships\n\n**JSONB Functions**:\n```sql\n-- Update nested value\nUPDATE products SET attributes = \n  jsonb_set(attributes, '{specs,ram}', '32')\nWHERE id = 1;\n\n-- Remove key\nUPDATE products SET attributes = attributes - 'brand';\n\n-- Aggregate to JSONB\nSELECT jsonb_agg(name) FROM products;\nSELECT jsonb_object_agg(id, name) FROM products;\n```"
    },
    {
      "questionId": "d0e9f8a7-6543-2109-87cb-afedc0987654",
      "locale": "vi",
      "title": "Query optimization trong PostgreSQL: EXPLAIN ANALYZE, index selection, và common performance issues",
      "content": "Giải thích cách optimize queries trong PostgreSQL với EXPLAIN ANALYZE.",
      "answer": "**EXPLAIN ANALYZE**:\n```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders\nWHERE user_id = 123\nAND created_at > '2024-01-01';\n```\n\n**Output Key Metrics**:\n- **cost**: Estimated startup..total cost\n- **rows**: Estimated rows returned\n- **actual time**: Real execution time (ms)\n- **loops**: Number of iterations\n\n**Scan Types** (fast to slow):\n1. Index Scan / Index Only Scan\n2. Bitmap Index Scan\n3. Sequential Scan (Seq Scan)\n\n**Common Issues**:\n\n1. **Missing Index**:\n```sql\n-- Slow: Seq Scan\nEXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 123;\n\n-- Fix\nCREATE INDEX idx_orders_user ON orders(user_id);\n```\n\n2. **Wrong Index** (low selectivity):\n```sql\n-- Index on boolean với 50/50 distribution\nCREATE INDEX idx_active ON users(is_active);  -- Useless\n\n-- Better: Partial index\nCREATE INDEX idx_active_users ON users(id) WHERE is_active = true;\n```\n\n3. **Index Not Used** (type mismatch):\n```sql\n-- user_id is INTEGER, but comparing to string\nSELECT * FROM orders WHERE user_id = '123';  -- May skip index\n```\n\n4. **Too Many Rows**:\n```sql\n-- Add LIMIT, use pagination\nSELECT * FROM orders ORDER BY created_at DESC LIMIT 20;\n```\n\n**Composite Index Order**:\n```sql\n-- For: WHERE user_id = ? AND status = ? ORDER BY created_at\nCREATE INDEX idx_orders_composite \nON orders(user_id, status, created_at DESC);\n```\n\n**Useful Commands**:\n```sql\n-- Table statistics\nANALYZE orders;\n\n-- Index usage\nSELECT * FROM pg_stat_user_indexes WHERE relname = 'orders';\n\n-- Missing indexes\nSELECT * FROM pg_stat_user_tables WHERE seq_scan > idx_scan;\n```"
    },
    {
      "questionId": "e1f0a9b8-7654-3210-98dc-bafed1098765",
      "locale": "vi",
      "title": "Thiết kế rate limiter system",
      "content": "Thiết kế một rate limiter cho API. Giải thích các algorithms và scaling considerations.",
      "answer": "**Algorithms**:\n\n1. **Token Bucket**:\n```ts\nclass TokenBucket {\n  private tokens: number;\n  private lastRefill: number;\n\n  constructor(\n    private capacity: number,\n    private refillRate: number // tokens per second\n  ) {\n    this.tokens = capacity;\n    this.lastRefill = Date.now();\n  }\n\n  tryConsume(): boolean {\n    this.refill();\n    if (this.tokens >= 1) {\n      this.tokens -= 1;\n      return true;\n    }\n    return false;\n  }\n\n  private refill() {\n    const now = Date.now();\n    const elapsed = (now - this.lastRefill) / 1000;\n    this.tokens = Math.min(this.capacity, this.tokens + elapsed * this.refillRate);\n    this.lastRefill = now;\n  }\n}\n```\n\n2. **Sliding Window Log**:\n```ts\nclass SlidingWindowLog {\n  private requests: number[] = [];\n\n  constructor(\n    private windowMs: number,\n    private maxRequests: number\n  ) {}\n\n  tryConsume(): boolean {\n    const now = Date.now();\n    const windowStart = now - this.windowMs;\n    \n    // Remove old requests\n    this.requests = this.requests.filter(t => t > windowStart);\n    \n    if (this.requests.length < this.maxRequests) {\n      this.requests.push(now);\n      return true;\n    }\n    return false;\n  }\n}\n```\n\n**Redis Implementation**:\n```ts\nasync function checkRateLimit(userId: string): Promise<boolean> {\n  const key = `rate:${userId}`;\n  const limit = 100;\n  const window = 60; // seconds\n\n  const current = await redis.incr(key);\n  if (current === 1) {\n    await redis.expire(key, window);\n  }\n\n  return current <= limit;\n}\n```\n\n**Distributed Rate Limiting**:\n```ts\n// Sliding window counter with Redis\nasync function slidingWindowCounter(userId: string): Promise<boolean> {\n  const now = Date.now();\n  const windowSize = 60000; // 1 minute\n  const limit = 100;\n\n  const currentWindow = Math.floor(now / windowSize);\n  const previousWindow = currentWindow - 1;\n  const currentKey = `rate:${userId}:${currentWindow}`;\n  const previousKey = `rate:${userId}:${previousWindow}`;\n\n  const [current, previous] = await redis.mget(currentKey, previousKey);\n  const weight = (now % windowSize) / windowSize;\n  const count = (parseInt(previous) || 0) * (1 - weight) + (parseInt(current) || 0);\n\n  if (count >= limit) return false;\n\n  await redis.incr(currentKey);\n  await redis.expire(currentKey, windowSize * 2 / 1000);\n  return true;\n}\n```\n\n**Response Headers**:\n```\nX-RateLimit-Limit: 100\nX-RateLimit-Remaining: 45\nX-RateLimit-Reset: 1234567890\n```"
    },
    {
      "questionId": "f2a1b0c9-8765-4321-09ed-cbafe2109876",
      "locale": "vi",
      "title": "Thiết kế distributed task queue",
      "content": "Thiết kế một distributed task queue system. Giải thích architecture và reliability patterns.",
      "answer": "**Requirements**:\n- Reliable task delivery\n- At-least-once execution\n- Priority queues\n- Delayed/scheduled tasks\n- Dead letter queue\n- Monitoring\n\n**Architecture**:\n```\n[Producers] → [Message Broker] → [Workers]\n                  ↓\n            [Dead Letter Queue]\n                  ↓\n            [Monitoring/Alerts]\n```\n\n**Redis Queue (BullMQ)**:\n```ts\nimport { Queue, Worker } from 'bullmq';\n\n// Producer\nconst emailQueue = new Queue('email', {\n  connection: { host: 'redis', port: 6379 },\n});\n\nawait emailQueue.add('send-welcome', {\n  to: 'user@example.com',\n  template: 'welcome',\n}, {\n  attempts: 3,\n  backoff: { type: 'exponential', delay: 1000 },\n  delay: 5000, // Delayed job\n  priority: 1, // Higher = lower priority\n});\n\n// Worker\nconst worker = new Worker('email', async (job) => {\n  await sendEmail(job.data);\n}, {\n  connection: { host: 'redis', port: 6379 },\n  concurrency: 5,\n});\n\nworker.on('completed', (job) => console.log(`Job ${job.id} completed`));\nworker.on('failed', (job, err) => console.error(`Job ${job.id} failed`, err));\n```\n\n**Reliability Patterns**:\n\n1. **Idempotency**:\n```ts\nasync function processPayment(job) {\n  const { orderId, amount } = job.data;\n  \n  // Check if already processed\n  const existing = await db.payments.findOne({ orderId });\n  if (existing) return existing;\n  \n  // Process and record\n  const payment = await processAndSave(orderId, amount);\n  return payment;\n}\n```\n\n2. **Dead Letter Queue**:\n```ts\nconst worker = new Worker('email', processor, {\n  settings: {\n    maxStalledCount: 3,\n  },\n});\n\nworker.on('failed', async (job, err) => {\n  if (job.attemptsMade >= job.opts.attempts) {\n    await deadLetterQueue.add('failed-email', {\n      originalJob: job.data,\n      error: err.message,\n    });\n  }\n});\n```\n\n3. **Graceful Shutdown**:\n```ts\nprocess.on('SIGTERM', async () => {\n  await worker.close();\n  process.exit(0);\n});\n```"
    },
    {
      "questionId": "a3b2c1d0-9876-5432-10fe-dcbaf3210987",
      "locale": "vi",
      "title": "Integration testing cho NestJS: database setup, authentication, và testing strategies",
      "content": "Giải thích cách viết integration tests cho NestJS applications.",
      "answer": "**Test Setup**:\n```ts\nimport { Test } from '@nestjs/testing';\nimport { INestApplication } from '@nestjs/common';\nimport * as request from 'supertest';\nimport { AppModule } from '../src/app.module';\n\ndescribe('UsersController (e2e)', () => {\n  let app: INestApplication;\n\n  beforeAll(async () => {\n    const moduleRef = await Test.createTestingModule({\n      imports: [AppModule],\n    }).compile();\n\n    app = moduleRef.createNestApplication();\n    await app.init();\n  });\n\n  afterAll(async () => {\n    await app.close();\n  });\n\n  it('/users (GET)', () => {\n    return request(app.getHttpServer())\n      .get('/users')\n      .expect(200)\n      .expect((res) => {\n        expect(Array.isArray(res.body)).toBe(true);\n      });\n  });\n});\n```\n\n**Database Setup**:\n```ts\nimport { DataSource } from 'typeorm';\n\ndescribe('UsersService Integration', () => {\n  let dataSource: DataSource;\n  let usersService: UsersService;\n\n  beforeAll(async () => {\n    const module = await Test.createTestingModule({\n      imports: [\n        TypeOrmModule.forRoot({\n          type: 'postgres',\n          host: 'localhost',\n          database: 'test_db',\n          synchronize: true,\n          entities: [User],\n        }),\n        TypeOrmModule.forFeature([User]),\n      ],\n      providers: [UsersService],\n    }).compile();\n\n    dataSource = module.get(DataSource);\n    usersService = module.get(UsersService);\n  });\n\n  beforeEach(async () => {\n    await dataSource.synchronize(true); // Clean database\n  });\n\n  afterAll(async () => {\n    await dataSource.destroy();\n  });\n});\n```\n\n**Testing with Authentication**:\n```ts\ndescribe('Protected Routes', () => {\n  let authToken: string;\n\n  beforeAll(async () => {\n    // Login to get token\n    const response = await request(app.getHttpServer())\n      .post('/auth/login')\n      .send({ email: 'test@test.com', password: 'password' });\n    authToken = response.body.accessToken;\n  });\n\n  it('/users/profile (GET) - authenticated', () => {\n    return request(app.getHttpServer())\n      .get('/users/profile')\n      .set('Authorization', `Bearer ${authToken}`)\n      .expect(200);\n  });\n\n  it('/users/profile (GET) - unauthorized', () => {\n    return request(app.getHttpServer())\n      .get('/users/profile')\n      .expect(401);\n  });\n});\n```\n\n**Test Factories**:\n```ts\nconst createTestUser = async (overrides = {}) => {\n  return await userRepository.save({\n    email: 'test@example.com',\n    name: 'Test User',\n    ...overrides,\n  });\n};\n```"
    },
    {
      "questionId": "b4c3d2e1-0987-6543-21af-edbca4321098",
      "locale": "vi",
      "title": "Property-based testing và fuzzing: finding edge cases automatically",
      "content": "Giải thích property-based testing và fuzzing để tìm edge cases tự động.",
      "answer": "**Property-based Testing**:\n- Generate random inputs\n- Test properties that should always hold\n- Find edge cases automatically\n\n**fast-check Example**:\n```ts\nimport * as fc from 'fast-check';\n\n// Property: reverse twice = original\ntest('reverse is involutory', () => {\n  fc.assert(\n    fc.property(fc.array(fc.integer()), (arr) => {\n      const reversed = [...arr].reverse().reverse();\n      return JSON.stringify(reversed) === JSON.stringify(arr);\n    })\n  );\n});\n\n// Property: sort is idempotent\ntest('sort is idempotent', () => {\n  fc.assert(\n    fc.property(fc.array(fc.integer()), (arr) => {\n      const sorted1 = [...arr].sort((a, b) => a - b);\n      const sorted2 = [...sorted1].sort((a, b) => a - b);\n      return JSON.stringify(sorted1) === JSON.stringify(sorted2);\n    })\n  );\n});\n```\n\n**Testing Business Logic**:\n```ts\ntest('discount never exceeds original price', () => {\n  fc.assert(\n    fc.property(\n      fc.record({\n        price: fc.float({ min: 0, max: 10000 }),\n        discountPercent: fc.integer({ min: 0, max: 100 }),\n      }),\n      ({ price, discountPercent }) => {\n        const discounted = applyDiscount(price, discountPercent);\n        return discounted >= 0 && discounted <= price;\n      }\n    )\n  );\n});\n```\n\n**Custom Arbitraries**:\n```ts\nconst userArbitrary = fc.record({\n  email: fc.emailAddress(),\n  age: fc.integer({ min: 0, max: 150 }),\n  name: fc.string({ minLength: 1, maxLength: 100 }),\n});\n\ntest('user validation', () => {\n  fc.assert(\n    fc.property(userArbitrary, (user) => {\n      const result = validateUser(user);\n      return result.isValid || result.errors.length > 0;\n    })\n  );\n});\n```\n\n**Shrinking**:\n- When test fails, fast-check shrinks to minimal failing case\n- Easier to understand root cause\n\n**Use Cases**:\n- Serialization/deserialization roundtrips\n- Mathematical properties\n- Parser/formatter pairs\n- State machine transitions\n- Concurrent operations"
    },
    {
      "questionId": "c5d4e3f2-1098-7654-32ba-fecdb5432109",
      "locale": "vi",
      "title": "Testing React components: React Testing Library patterns và common anti-patterns",
      "content": "Giải thích cách test React components với React Testing Library.",
      "answer": "**Basic Testing**:\n```tsx\nimport { render, screen, fireEvent } from '@testing-library/react';\nimport { Counter } from './Counter';\n\ntest('increments counter', () => {\n  render(<Counter />);\n  \n  const button = screen.getByRole('button', { name: /increment/i });\n  fireEvent.click(button);\n  \n  expect(screen.getByText('Count: 1')).toBeInTheDocument();\n});\n```\n\n**Query Priority** (best to worst):\n1. `getByRole` — accessible\n2. `getByLabelText` — forms\n3. `getByPlaceholderText` — inputs\n4. `getByText` — non-interactive\n5. `getByTestId` — last resort\n\n**Async Testing**:\n```tsx\nimport { render, screen, waitFor } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\n\ntest('loads user data', async () => {\n  const user = userEvent.setup();\n  render(<UserProfile userId=\"123\" />);\n\n  // Wait for loading to finish\n  expect(screen.getByText(/loading/i)).toBeInTheDocument();\n  \n  await waitFor(() => {\n    expect(screen.getByText('John Doe')).toBeInTheDocument();\n  });\n});\n\ntest('form submission', async () => {\n  const user = userEvent.setup();\n  const onSubmit = jest.fn();\n  render(<Form onSubmit={onSubmit} />);\n\n  await user.type(screen.getByLabelText(/email/i), 'test@test.com');\n  await user.click(screen.getByRole('button', { name: /submit/i }));\n\n  expect(onSubmit).toHaveBeenCalledWith({ email: 'test@test.com' });\n});\n```\n\n**Anti-patterns**:\n```tsx\n// Bad: Testing implementation details\nexpect(component.state.isOpen).toBe(true);\n\n// Good: Test behavior\nexpect(screen.getByRole('dialog')).toBeVisible();\n\n// Bad: Using getByTestId for everything\n<button data-testid=\"submit-button\">Submit</button>\nscreen.getByTestId('submit-button');\n\n// Good: Use accessible queries\n<button>Submit</button>\nscreen.getByRole('button', { name: /submit/i });\n\n// Bad: Waiting arbitrary time\nawait new Promise(r => setTimeout(r, 1000));\n\n// Good: Wait for element\nawait screen.findByText('Loaded');\n```\n\n**Testing Custom Hooks**:\n```tsx\nimport { renderHook, act } from '@testing-library/react';\nimport { useCounter } from './useCounter';\n\ntest('useCounter', () => {\n  const { result } = renderHook(() => useCounter());\n  \n  expect(result.current.count).toBe(0);\n  \n  act(() => {\n    result.current.increment();\n  });\n  \n  expect(result.current.count).toBe(1);\n});\n```"
    },
    {
      "questionId": "d6e5f4a3-2109-8765-43cb-afed6543210a",
      "locale": "vi",
      "title": "E2E testing với Playwright: page objects, fixtures, và parallel execution",
      "content": "Giải thích cách viết E2E tests với Playwright.",
      "answer": "**Basic Test**:\n```ts\nimport { test, expect } from '@playwright/test';\n\ntest('user can login', async ({ page }) => {\n  await page.goto('/login');\n  \n  await page.fill('[name=\"email\"]', 'user@example.com');\n  await page.fill('[name=\"password\"]', 'password');\n  await page.click('button[type=\"submit\"]');\n  \n  await expect(page).toHaveURL('/dashboard');\n  await expect(page.locator('h1')).toContainText('Welcome');\n});\n```\n\n**Page Object Model**:\n```ts\n// pages/login.page.ts\nexport class LoginPage {\n  constructor(private page: Page) {}\n\n  async goto() {\n    await this.page.goto('/login');\n  }\n\n  async login(email: string, password: string) {\n    await this.page.fill('[name=\"email\"]', email);\n    await this.page.fill('[name=\"password\"]', password);\n    await this.page.click('button[type=\"submit\"]');\n  }\n}\n\n// tests/login.spec.ts\ntest('login flow', async ({ page }) => {\n  const loginPage = new LoginPage(page);\n  await loginPage.goto();\n  await loginPage.login('user@example.com', 'password');\n  await expect(page).toHaveURL('/dashboard');\n});\n```\n\n**Fixtures**:\n```ts\n// fixtures.ts\nimport { test as base } from '@playwright/test';\nimport { LoginPage } from './pages/login.page';\n\ntype Fixtures = {\n  loginPage: LoginPage;\n  authenticatedPage: Page;\n};\n\nexport const test = base.extend<Fixtures>({\n  loginPage: async ({ page }, use) => {\n    const loginPage = new LoginPage(page);\n    await use(loginPage);\n  },\n\n  authenticatedPage: async ({ page }, use) => {\n    await page.goto('/login');\n    await page.fill('[name=\"email\"]', 'test@test.com');\n    await page.fill('[name=\"password\"]', 'password');\n    await page.click('button[type=\"submit\"]');\n    await page.waitForURL('/dashboard');\n    await use(page);\n  },\n});\n```\n\n**Parallel Execution**:\n```ts\n// playwright.config.ts\nexport default defineConfig({\n  workers: process.env.CI ? 2 : undefined,\n  fullyParallel: true,\n  retries: process.env.CI ? 2 : 0,\n});\n```\n\n**Visual Testing**:\n```ts\ntest('homepage screenshot', async ({ page }) => {\n  await page.goto('/');\n  await expect(page).toHaveScreenshot('homepage.png');\n});\n```"
    },
    {
      "questionId": "88727c4a-b939-4469-a304-710db031bc09",
      "locale": "vi",
      "title": "Git tags cho release management như thế nào?",
      "content": "Giải thích cách sử dụng Git tags để quản lý releases.",
      "answer": "**Tag Types**:\n\n1. **Lightweight tags** (just a pointer):\n```bash\ngit tag v1.0.0\n```\n\n2. **Annotated tags** (recommended for releases):\n```bash\ngit tag -a v1.0.0 -m \"Release version 1.0.0\"\n```\n\n**Creating Tags**:\n```bash\n# Tag current commit\ngit tag -a v1.0.0 -m \"Release 1.0.0\"\n\n# Tag specific commit\ngit tag -a v1.0.0 abc1234 -m \"Release 1.0.0\"\n\n# Tag with GPG signature\ngit tag -s v1.0.0 -m \"Signed release 1.0.0\"\n```\n\n**Listing Tags**:\n```bash\ngit tag                    # List all tags\ngit tag -l \"v1.*\"          # Filter tags\ngit show v1.0.0            # Show tag details\ngit tag -l --sort=-v:refname  # Sort by version\n```\n\n**Pushing Tags**:\n```bash\ngit push origin v1.0.0     # Push specific tag\ngit push origin --tags     # Push all tags\ngit push --follow-tags     # Push commits and annotated tags\n```\n\n**Deleting Tags**:\n```bash\ngit tag -d v1.0.0          # Delete local\ngit push origin :refs/tags/v1.0.0  # Delete remote\ngit push origin --delete v1.0.0    # Delete remote (alt)\n```\n\n**Semantic Versioning**:\n```\nMAJOR.MINOR.PATCH\nv1.0.0 → v1.0.1 (patch: bug fixes)\nv1.0.1 → v1.1.0 (minor: new features)\nv1.1.0 → v2.0.0 (major: breaking changes)\n```\n\n**Release Workflow**:\n```bash\n# 1. Update version in package.json\nnpm version patch  # or minor/major\n\n# 2. This creates commit and tag automatically\n# 3. Push with tags\ngit push --follow-tags\n\n# 4. Create GitHub release from tag\ngh release create v1.0.0 --generate-notes\n```\n\n**Checkout Tag**:\n```bash\ngit checkout v1.0.0        # Detached HEAD\ngit checkout -b hotfix v1.0.0  # New branch from tag\n```"
    },
    {
      "questionId": "94a2c240-b76f-42de-b16a-5d8e2f804563",
      "locale": "vi",
      "title": "Git bisect để tìm commit gây bug như thế nào?",
      "content": "Giải thích cách sử dụng git bisect để tìm commit đã introduce bug.",
      "answer": "**Basic Bisect**:\n```bash\n# Start bisect\ngit bisect start\n\n# Mark current (broken) as bad\ngit bisect bad\n\n# Mark known good commit\ngit bisect good v1.0.0\n\n# Git checks out middle commit\n# Test and mark\ngit bisect good  # or bad\n\n# Repeat until found\n# Git shows: \"abc123 is the first bad commit\"\n\n# End bisect\ngit bisect reset\n```\n\n**Automated Bisect**:\n```bash\n# Run script to determine good/bad\ngit bisect start HEAD v1.0.0\ngit bisect run npm test\n\n# Script exit codes:\n# 0 = good\n# 1-124, 126-127 = bad\n# 125 = skip (can't test this commit)\n```\n\n**Custom Test Script**:\n```bash\n#!/bin/bash\n# test-bug.sh\n\nnpm install --silent\nif npm test 2>&1 | grep -q \"specific error\"; then\n  exit 1  # bad\nelse\n  exit 0  # good\nfi\n\n# Run\ngit bisect run ./test-bug.sh\n```\n\n**Skip Commits**:\n```bash\n# If commit can't be tested (broken build)\ngit bisect skip\n\n# Skip range\ngit bisect skip v1.0.1..v1.0.5\n```\n\n**View Progress**:\n```bash\ngit bisect log    # Show bisect history\ngit bisect visualize  # Open in gitk\n```\n\n**Example Session**:\n```bash\n$ git bisect start\n$ git bisect bad HEAD\n$ git bisect good v2.0.0\nBisecting: 10 revisions left to test\n[abc123] Add feature X\n\n$ npm test\n# Tests pass\n$ git bisect good\nBisecting: 5 revisions left to test\n\n$ npm test\n# Tests fail\n$ git bisect bad\nBisecting: 2 revisions left to test\n\n# Continue until:\nabc123 is the first bad commit\n\n$ git bisect reset\n```\n\n**Tips**:\n- Start with known good và bad commits\n- Use automated scripts khi có thể\n- Check compile/build success trước khi test"
    },
    {
      "questionId": "cd85cecb-d027-4153-a111-c592ed3efc49",
      "locale": "vi",
      "title": "Git diff hiệu quả và tạo/apply patches như thế nào?",
      "content": "Giải thích cách sử dụng git diff và tạo/apply patches.",
      "answer": "**Basic Diff**:\n```bash\n# Working directory vs staged\ngit diff\n\n# Staged vs last commit\ngit diff --staged\ngit diff --cached\n\n# Between commits\ngit diff abc123..def456\ngit diff HEAD~3..HEAD\n\n# Specific file\ngit diff -- path/to/file.ts\n```\n\n**Diff Options**:\n```bash\n# Word diff (better for prose)\ngit diff --word-diff\n\n# Stats only\ngit diff --stat\n\n# Name only\ngit diff --name-only\ngit diff --name-status\n\n# Ignore whitespace\ngit diff -w\ngit diff --ignore-all-space\n\n# Context lines\ngit diff -U5  # 5 lines context (default 3)\n```\n\n**Compare Branches**:\n```bash\n# Changes in feature not in main\ngit diff main...feature\n\n# Changes since branch point\ngit diff $(git merge-base main feature)..feature\n```\n\n**Creating Patches**:\n```bash\n# Single commit\ngit format-patch -1 HEAD\n\n# Range of commits\ngit format-patch main..feature\n\n# Output to directory\ngit format-patch -o patches/ main..feature\n\n# Simple diff patch\ngit diff > changes.patch\ngit diff --staged > staged.patch\n```\n\n**Applying Patches**:\n```bash\n# Apply format-patch output\ngit am 0001-Add-feature.patch\ngit am patches/*.patch\n\n# Apply diff patch\ngit apply changes.patch\n\n# Check if patch applies\ngit apply --check changes.patch\n\n# Apply with 3-way merge\ngit am -3 patch.patch\n```\n\n**Interactive Patch**:\n```bash\n# Stage hunks interactively\ngit add -p\n\n# Unstage hunks\ngit reset -p\n\n# Checkout hunks\ngit checkout -p\n```\n\n**Useful Aliases**:\n```bash\ngit config --global alias.d \"diff --word-diff\"\ngit config --global alias.ds \"diff --staged\"\ngit config --global alias.dc \"diff --cached\"\n```"
    },
    {
      "questionId": "f7efc18f-d833-4cf6-a030-aba1b685e281",
      "locale": "vi",
      "title": "Sử dụng git stash hiệu quả như thế nào?",
      "content": "Giải thích cách sử dụng git stash để quản lý work-in-progress changes.",
      "answer": "**Basic Stash**:\n```bash\n# Stash working directory và staged changes\ngit stash\ngit stash push\n\n# Stash với message\ngit stash push -m \"WIP: feature X\"\n\n# Stash including untracked files\ngit stash -u\ngit stash --include-untracked\n\n# Stash including ignored files\ngit stash -a\ngit stash --all\n```\n\n**Listing Stashes**:\n```bash\ngit stash list\n# stash@{0}: WIP on main: abc123 message\n# stash@{1}: On feature: def456 another message\n\ngit stash show        # Show diff stats\ngit stash show -p     # Show full diff\ngit stash show stash@{1}\n```\n\n**Applying Stashes**:\n```bash\n# Apply most recent, keep in stash\ngit stash apply\n\n# Apply and remove from stash\ngit stash pop\n\n# Apply specific stash\ngit stash apply stash@{2}\ngit stash pop stash@{2}\n```\n\n**Selective Stashing**:\n```bash\n# Stash specific files\ngit stash push -m \"message\" -- file1.ts file2.ts\n\n# Interactive stash (select hunks)\ngit stash -p\n```\n\n**Managing Stashes**:\n```bash\n# Drop specific stash\ngit stash drop stash@{1}\n\n# Clear all stashes\ngit stash clear\n\n# Create branch from stash\ngit stash branch new-feature stash@{0}\n```\n\n**Stash Workflow Example**:\n```bash\n# Working on feature, urgent bug reported\ngit stash push -m \"WIP: feature X\"\n\n# Fix bug\ngit checkout -b hotfix\n# ... make fixes ...\ngit commit -m \"Fix critical bug\"\ngit checkout main\ngit merge hotfix\n\n# Return to feature\ngit checkout feature\ngit stash pop\n```\n\n**Tips**:\n- Use descriptive messages: `git stash push -m \"description\"`\n- Don't let stashes pile up — apply or drop regularly\n- `git stash branch` is useful for complex stashes"
    },
    {
      "questionId": "929722e2-db30-488c-ac57-51655f242c8d",
      "locale": "vi",
      "title": "Interactive rebase để clean up commit history như thế nào?",
      "content": "Giải thích cách sử dụng interactive rebase để clean up commits trước khi merge.",
      "answer": "**Start Interactive Rebase**:\n```bash\ngit rebase -i HEAD~5     # Last 5 commits\ngit rebase -i main       # Since branching from main\ngit rebase -i abc123     # Since specific commit\n```\n\n**Editor Commands**:\n```\npick abc1234 First commit\npick def5678 Second commit\npick ghi9012 Third commit\n\n# Commands:\n# p, pick = use commit\n# r, reword = use commit, edit message\n# e, edit = use commit, stop for amending\n# s, squash = meld into previous, keep message\n# f, fixup = meld into previous, discard message\n# d, drop = remove commit\n# x, exec = run command\n```\n\n**Common Operations**:\n\n1. **Squash commits**:\n```\npick abc1234 Add feature\nsquash def5678 Fix typo\nsquash ghi9012 More fixes\n# Results in one commit with combined messages\n```\n\n2. **Fixup** (squash without message):\n```\npick abc1234 Add feature\nfixup def5678 Fix typo\nfixup ghi9012 More fixes\n# Results in one commit with first message only\n```\n\n3. **Reorder commits**:\n```\n# Before:\npick abc1234 Feature A\npick def5678 Feature B\npick ghi9012 Fix for A\n\n# After (move fix next to feature):\npick abc1234 Feature A\npick ghi9012 Fix for A\npick def5678 Feature B\n```\n\n4. **Edit commit**:\n```\nedit abc1234 Commit to modify\npick def5678 Next commit\n\n# Git stops at abc1234\ngit add/rm files\ngit commit --amend\ngit rebase --continue\n```\n\n**Autosquash**:\n```bash\n# Create fixup commit\ngit commit --fixup abc1234\n\n# Later, auto-squash\ngit rebase -i --autosquash main\n```\n\n**Abort/Continue**:\n```bash\ngit rebase --abort      # Cancel rebase\ngit rebase --continue   # After resolving conflicts\ngit rebase --skip       # Skip current commit\n```\n\n**Best Practice**: Clean up commits before PR merge, not after."
    },
    {
      "questionId": "5f388431-8816-423f-a128-188e73400bc5",
      "locale": "vi",
      "title": ".gitignore hoạt động như thế nào và các patterns phổ biến?",
      "content": "Giải thích cách .gitignore hoạt động và các patterns thường dùng.",
      "answer": "**Basic Patterns**:\n```gitignore\n# Ignore file\nfile.txt\n\n# Ignore directory\nnode_modules/\n\n# Wildcard\n*.log\n*.tmp\n\n# Double asterisk (any directory depth)\n**/logs\nlogs/**/debug.log\n\n# Negation (include)\n!important.log\n\n# Character range\n[0-9].txt\n```\n\n**Common .gitignore**:\n```gitignore\n# Dependencies\nnode_modules/\n\n# Build output\ndist/\nbuild/\n.next/\n\n# Environment\n.env\n.env.local\n.env*.local\n\n# IDE\n.idea/\n.vscode/\n*.swp\n\n# OS\n.DS_Store\nThumbs.db\n\n# Logs\n*.log\nlogs/\n\n# Coverage\ncoverage/\n\n# Cache\n.cache/\n.turbo/\n```\n\n**Gitignore Levels**:\n1. **Repository**: `.gitignore` in repo\n2. **Global**: `~/.gitignore_global`\n3. **Local**: `.git/info/exclude`\n\n**Setup Global Gitignore**:\n```bash\ngit config --global core.excludesFile ~/.gitignore_global\n\n# ~/.gitignore_global\n.DS_Store\n.idea/\n*.swp\n```\n\n**Ignore Already Tracked Files**:\n```bash\n# Remove from tracking, keep file\ngit rm --cached file.txt\ngit rm -r --cached directory/\n\n# Then add to .gitignore\necho \"file.txt\" >> .gitignore\n```\n\n**Check What's Ignored**:\n```bash\ngit status --ignored\ngit check-ignore -v file.txt\n```\n\n**Force Add Ignored File**:\n```bash\ngit add -f ignored-file.txt\n```\n\n**Template .gitignore**:\n```bash\n# Use GitHub's templates\ncurl https://raw.githubusercontent.com/github/gitignore/main/Node.gitignore > .gitignore\n\n# Or gitignore.io\ncurl gitignore.io/api/node,react > .gitignore\n```"
    },
    {
      "questionId": "8f338d93-d05d-477e-9ee5-2cae82c9bf8f",
      "locale": "vi",
      "title": "Git lưu trữ data internally như thế nào? Giải thích blobs, trees, và commits.",
      "content": "Giải thích cách Git lưu trữ data với blobs, trees, và commits.",
      "answer": "**Git Object Types**:\n\n1. **Blob** — File content:\n```bash\n# Store content, get SHA-1\necho \"Hello\" | git hash-object -w --stdin\n# Returns: ce013625030ba8dba906f756967f9e9ca394464a\n\n# View blob\ngit cat-file -p ce0136\n# Output: Hello\n```\n\n2. **Tree** — Directory structure:\n```bash\ngit cat-file -p HEAD^{tree}\n# 100644 blob abc123    README.md\n# 040000 tree def456    src\n```\n\n3. **Commit** — Snapshot with metadata:\n```bash\ngit cat-file -p HEAD\n# tree abc123\n# parent def456\n# author Name <email> timestamp\n# committer Name <email> timestamp\n#\n# Commit message\n```\n\n**Object Storage**:\n```\n.git/objects/\n├── ce/\n│   └── 013625030ba8dba906f756967f9e9ca394464a\n├── pack/\n│   ├── pack-abc123.idx\n│   └── pack-abc123.pack\n└── info/\n```\n\n**How Commits Link**:\n```\nCommit (HEAD)\n  ↓ tree\nTree (root)\n  ↓ blob, tree\nBlob (file content)\n```\n\n**References**:\n```bash\n# Branches are files pointing to commits\ncat .git/refs/heads/main\n# abc123def456...\n\n# HEAD points to current branch\ncat .git/HEAD\n# ref: refs/heads/main\n```\n\n**Useful Commands**:\n```bash\n# Count objects\ngit count-objects -v\n\n# Verify integrity\ngit fsck\n\n# View object type\ngit cat-file -t abc123\n\n# View object content\ngit cat-file -p abc123\n\n# Show all refs\ngit show-ref\n```\n\n**Pack Files**:\n- Git packs loose objects periodically\n- Delta compression between similar objects\n- `.pack` contains compressed objects\n- `.idx` is index for fast lookup\n\n```bash\n# Manual pack\ngit gc\n\n# Verify pack\ngit verify-pack -v .git/objects/pack/pack-*.idx\n```"
    },
    {
      "questionId": "5f9d195c-e1b7-4f15-a418-73e657d705c3",
      "locale": "vi",
      "title": "Git hooks là gì và dùng trong team như thế nào?",
      "content": "Giải thích Git hooks và cách sử dụng chúng trong team development.",
      "answer": "**Git Hooks Location**:\n```\n.git/hooks/\n├── pre-commit.sample\n├── pre-push.sample\n├── commit-msg.sample\n└── ...\n```\n\n**Common Hooks**:\n\n1. **pre-commit** — Before commit:\n```bash\n#!/bin/sh\nnpm run lint\nnpm run test\n```\n\n2. **commit-msg** — Validate message:\n```bash\n#!/bin/sh\nif ! grep -qE \"^(feat|fix|docs|style|refactor|test|chore): .+\" \"$1\"; then\n  echo \"Invalid commit message format\"\n  exit 1\nfi\n```\n\n3. **pre-push** — Before push:\n```bash\n#!/bin/sh\nnpm run test:all\n```\n\n**Sharing Hooks with Husky**:\n```bash\nnpm install husky -D\nnpx husky init\n```\n\n```json\n// package.json\n{\n  \"scripts\": {\n    \"prepare\": \"husky\"\n  }\n}\n```\n\n```bash\n# .husky/pre-commit\nnpm run lint-staged\n\n# .husky/commit-msg\nnpx --no -- commitlint --edit $1\n```\n\n**lint-staged** — Only lint staged files:\n```json\n// package.json\n{\n  \"lint-staged\": {\n    \"*.{ts,tsx}\": [\"eslint --fix\", \"prettier --write\"],\n    \"*.{json,md}\": \"prettier --write\"\n  }\n}\n```\n\n**commitlint** — Enforce commit message format:\n```js\n// commitlint.config.js\nmodule.exports = {\n  extends: ['@commitlint/config-conventional'],\n};\n```\n\n**Server-side Hooks**:\n- `pre-receive` — Before accepting push\n- `post-receive` — After push (CI/CD trigger)\n- `update` — Per-branch validation\n\n**Skip Hooks** (use carefully):\n```bash\ngit commit --no-verify\ngit push --no-verify\n```\n\n**Best Practices**:\n- Keep hooks fast (<5 seconds)\n- Use lint-staged for large repos\n- Share via Husky or similar\n- Document bypass procedure"
    },
    {
      "questionId": "345346e6-2750-4f75-899d-f7f5ae163fe9",
      "locale": "vi",
      "title": "Git worktrees là gì và khi nào hữu ích?",
      "content": "Giải thích git worktrees và các use cases của chúng.",
      "answer": "**Git Worktree là gì?**\n- Multiple working directories từ một repository\n- Share `.git` folder\n- Work on multiple branches simultaneously\n\n**Basic Commands**:\n```bash\n# Create worktree\ngit worktree add ../project-hotfix hotfix-branch\n\n# Create with new branch\ngit worktree add -b feature-x ../project-feature main\n\n# List worktrees\ngit worktree list\n\n# Remove worktree\ngit worktree remove ../project-hotfix\n\n# Prune stale worktrees\ngit worktree prune\n```\n\n**Directory Structure**:\n```\n~/projects/\n├── my-project/           # Main worktree\n│   ├── .git/\n│   └── src/\n├── my-project-hotfix/    # Worktree for hotfix\n│   └── src/\n└── my-project-feature/   # Worktree for feature\n    └── src/\n```\n\n**Use Cases**:\n\n1. **Urgent hotfix while working on feature**:\n```bash\n# Currently on feature branch\ngit worktree add ../hotfix main\ncd ../hotfix\ngit checkout -b hotfix-123\n# Fix bug, commit, push\ncd ../main-project\ngit worktree remove ../hotfix\n```\n\n2. **Compare implementations**:\n```bash\ngit worktree add ../compare-old old-implementation\ngit worktree add ../compare-new new-implementation\n# Open both in editors, compare side-by-side\n```\n\n3. **Run tests on different branches**:\n```bash\ngit worktree add ../test-main main\ncd ../test-main && npm test &\ncd ../feature && npm test &\n```\n\n4. **Long-running tasks**:\n```bash\n# Build production while developing\ngit worktree add ../build-prod production\ncd ../build-prod && npm run build\n```\n\n**Limitations**:\n- Can't checkout same branch in multiple worktrees\n- Each worktree needs own node_modules\n\n**vs. git clone**:\n| Feature | Worktree | Clone |\n|---------|----------|-------|\n| Disk space | Shared .git | Separate |\n| Fetch once | Yes | No |\n| Branch lock | Yes | No |"
    },
    {
      "questionId": "2b70f23a-3224-470e-827b-9cd91adb3265",
      "locale": "vi",
      "title": "Best practices cho tạo và review pull requests?",
      "content": "Giải thích best practices khi tạo và review pull requests.",
      "answer": "**Creating PRs**:\n\n1. **Small, focused PRs**:\n```\n# Good: One feature/fix per PR\n# Bad: Multiple unrelated changes\n```\n\n2. **Clear title và description**:\n```markdown\n## Summary\n- Add user authentication\n- Implement JWT tokens\n\n## Changes\n- Added auth middleware\n- Created login/logout endpoints\n- Added user session management\n\n## Testing\n- [ ] Unit tests pass\n- [ ] Manual testing done\n\n## Screenshots (if UI changes)\n```\n\n3. **Self-review first**:\n```bash\n# Review your own diff\ngit diff main...feature\n\n# Check for:\n# - Debug code\n# - Console.logs\n# - Commented code\n# - Sensitive data\n```\n\n4. **Clean commit history**:\n```bash\n# Squash WIP commits\ngit rebase -i main\n```\n\n**Reviewing PRs**:\n\n1. **Understand context first**:\n- Read description\n- Check linked issues\n- Understand the \"why\"\n\n2. **Review systematically**:\n```\n1. Architecture/design\n2. Logic/correctness\n3. Error handling\n4. Tests\n5. Code style\n```\n\n3. **Constructive feedback**:\n```markdown\n# Good\n\"Consider using `useMemo` here to avoid recalculating on every render\"\n\n# Bad\n\"This is wrong\"\n```\n\n4. **Use suggestions**:\n```markdown\n```suggestion\nconst value = useMemo(() => expensiveCalc(data), [data]);\n```\n```\n\n**PR Checklist**:\n- [ ] Tests pass\n- [ ] No merge conflicts\n- [ ] Documentation updated\n- [ ] No console.logs\n- [ ] Error handling\n- [ ] Security considerations\n\n**GitHub CLI**:\n```bash\n# Create PR\ngh pr create --title \"Add feature\" --body \"Description\"\n\n# Review PR\ngh pr checkout 123\ngh pr review 123 --approve\ngh pr merge 123 --squash\n```"
    },
    {
      "questionId": "838e3763-4606-4497-97d6-4ea11e957343",
      "locale": "vi",
      "title": "Git cherry-pick là gì và khi nào nên dùng?",
      "content": "Giải thích git cherry-pick và các use cases của nó.",
      "answer": "**Cherry-pick là gì?**\n- Apply specific commit(s) từ branch này sang branch khác\n- Creates new commit(s) với same changes\n\n**Basic Usage**:\n```bash\n# Pick single commit\ngit cherry-pick abc123\n\n# Pick multiple commits\ngit cherry-pick abc123 def456 ghi789\n\n# Pick range\ngit cherry-pick abc123..def456  # Exclusive\ngit cherry-pick abc123^..def456 # Inclusive\n```\n\n**Options**:\n```bash\n# Don't commit (stage changes only)\ngit cherry-pick -n abc123\ngit cherry-pick --no-commit abc123\n\n# Edit commit message\ngit cherry-pick -e abc123\n\n# Append cherry-pick info to message\ngit cherry-pick -x abc123\n# Adds: (cherry picked from commit abc123)\n```\n\n**Use Cases**:\n\n1. **Hotfix from develop to main**:\n```bash\ngit checkout main\ngit cherry-pick abc123  # Bug fix commit from develop\n```\n\n2. **Backport feature to older version**:\n```bash\ngit checkout release-1.0\ngit cherry-pick feature-commit\n```\n\n3. **Extract commit from wrong branch**:\n```bash\n# Accidentally committed to main\ngit checkout feature-branch\ngit cherry-pick abc123\n\n# Remove from main\ngit checkout main\ngit reset --hard HEAD~1\n```\n\n**Handling Conflicts**:\n```bash\ngit cherry-pick abc123\n# CONFLICT\n\n# Resolve conflicts\ngit add resolved-file.ts\ngit cherry-pick --continue\n\n# Or abort\ngit cherry-pick --abort\n```\n\n**When NOT to use**:\n- Many commits (use merge/rebase instead)\n- Already merged branches (duplicates)\n- Public commits (creates duplicate history)\n\n**Best Practices**:\n- Use `-x` để track source commit\n- Cherry-pick oldest first (sequential)\n- Prefer merge/rebase for full branches"
    },
    {
      "questionId": "f9665075-6c79-4d7e-af2d-fbf603ea491a",
      "locale": "vi",
      "title": "Git reflog là gì và làm sao có thể cứu bạn khỏi mistakes?",
      "content": "Giải thích git reflog và cách dùng nó để recover từ mistakes.",
      "answer": "**Reflog là gì?**\n- Log mọi thay đổi của HEAD và branch refs\n- Local only (không push)\n- Kept for 90 days by default\n- Your safety net!\n\n**View Reflog**:\n```bash\ngit reflog\n# abc123 HEAD@{0}: commit: Add feature\n# def456 HEAD@{1}: checkout: moving from main to feature\n# ghi789 HEAD@{2}: reset: moving to HEAD~1\n\n# Specific branch\ngit reflog show feature\n\n# With dates\ngit reflog --date=iso\n```\n\n**Recovery Scenarios**:\n\n1. **Undo hard reset**:\n```bash\n# Oops!\ngit reset --hard HEAD~3\n\n# Recovery\ngit reflog\n# Find commit before reset: abc123 HEAD@{1}\ngit reset --hard abc123\n```\n\n2. **Recover deleted branch**:\n```bash\n# Oops!\ngit branch -D feature\n\n# Recovery\ngit reflog\n# Find last commit on feature: def456 HEAD@{5}\ngit checkout -b feature def456\n```\n\n3. **Undo bad rebase**:\n```bash\n# Oops! Rebase went wrong\ngit rebase --abort  # If still in progress\n\n# Or find pre-rebase state\ngit reflog\n# ghi789 HEAD@{10}: rebase (start)\ngit reset --hard HEAD@{10}\n```\n\n4. **Find lost commit**:\n```bash\n# Commit exists but not in any branch\ngit reflog | grep \"commit message\"\ngit checkout abc123\ngit branch recovered-work\n```\n\n**Reflog Reference Syntax**:\n```bash\nHEAD@{0}      # Current HEAD\nHEAD@{1}      # Previous HEAD\nmain@{1}      # Previous main\nHEAD@{yesterday}\nHEAD@{2.hours.ago}\n```\n\n**Config**:\n```bash\n# Change expiry (default 90 days)\ngit config gc.reflogExpire 180.days\ngit config gc.reflogExpireUnreachable 90.days\n```\n\n**Tips**:\n- Check reflog before panicking\n- `git fsck --lost-found` for dangling commits\n- Reflog is local — won't help with remote issues"
    }
  ]
}
